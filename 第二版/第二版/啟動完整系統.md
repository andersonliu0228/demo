# 啟動完整系統指南

## 系統架構

```
EA Trading 系統
├── 後端 (FastAPI)      → http://localhost:8000
├── 前端 (React + Vite) → http://localhost:5173
├── 資料庫 (PostgreSQL) → localhost:5432
└── 快取 (Redis)        → localhost:6379
```

---

## 快速啟動（5 步驟）

### 1. 啟動後端服務

```powershell
# 在專案根目錄
docker-compose up -d
```

等待 10-15 秒讓服務完全啟動。

### 2. 驗證後端運行

```powershell
# 檢查容器狀態
docker ps

# 測試健康檢查
Invoke-RestMethod -Uri "http://localhost:8000/health"
```

應該返回：`{ "status": "healthy" }`

### 3. 訪問 Swagger 文檔

打開瀏覽器訪問：http://localhost:8000/docs

可以查看所有 API 端點和測試。

### 4. 安裝前端依賴

```powershell
cd frontend
npm install
```

首次運行需要安裝依賴（約 2-3 分鐘）。

### 5. 啟動前端開發伺服器

```powershell
npm run dev
```

前端將運行在：http://localhost:5173

---

## 訪問系統

### 前端應用

1. 打開瀏覽器訪問：**http://localhost:5173**
2. 使用測試帳號登入：
   - 用戶名：`testuser`
   - 密碼：`testpass123`

### 後端 API

- Swagger 文檔：http://localhost:8000/docs
- ReDoc 文檔：http://localhost:8000/redoc
- 健康檢查：http://localhost:8000/health

---

## 完整測試流程

### 1. 登入系統

在前端登入頁面輸入測試帳號。

### 2. 查看儀表板

登入後會看到：
- 總持倉價值
- 跟單比例和設定
- Master 倉位
- 我的倉位
- 最近交易記錄
- 引擎狀態

### 3. 測試 Master 下單

1. 點擊右下角的閃電圖標（⚡）
2. 在測試控制台中：
   - 交易對：BTC/USDT
   - 倉位大小：1.0
   - 開倉價格：50000
3. 點擊「模擬 Master 下單」
4. 觀察數據變化：
   - Master 倉位立即更新
   - 3 秒後跟隨者倉位更新
   - 交易記錄新增一筆

### 4. 測試加倉

1. 在測試控制台中修改倉位大小為 2.0
2. 再次點擊「模擬 Master 下單」
3. 觀察跟隨者倉位從 0.1 增加到 0.2

### 5. 測試平倉

1. 在測試控制台中修改倉位大小為 0
2. 點擊「模擬 Master 下單」
3. 觀察跟隨者倉位歸零

---

## 系統監控

### 查看後端日誌

```powershell
# 實時查看日誌
docker logs -f ea_trading_backend

# 查看最近 100 行
docker logs ea_trading_backend --tail 100
```

### 查看資料庫

```powershell
# 連接到 PostgreSQL
docker exec -it ea_trading_postgres psql -U postgres -d ea_trading

# 查詢 Master 倉位
SELECT * FROM master_positions;

# 查詢跟隨者倉位
SELECT * FROM follower_positions;

# 查詢交易記錄
SELECT * FROM trade_logs ORDER BY timestamp DESC LIMIT 10;
```

### 查看 Redis

```powershell
# 連接到 Redis
docker exec -it ea_trading_redis redis-cli

# 查看所有 keys
KEYS *

# 查看特定 key
GET credential:1
```

---

## 停止系統

### 停止前端

在前端終端按 `Ctrl + C`

### 停止後端

```powershell
# 停止所有容器
docker-compose down

# 停止並刪除數據卷（慎用！）
docker-compose down -v
```

---

## 常見問題

### Q1: 前端無法連接後端

**A**: 檢查：
1. 後端是否運行：`docker ps`
2. 後端健康檢查：http://localhost:8000/health
3. CORS 設定是否正確
4. 瀏覽器 Console 中的錯誤訊息

### Q2: 登入失敗

**A**: 
1. 確認後端資料庫中有測試用戶
2. 檢查用戶名和密碼是否正確
3. 查看後端日誌：`docker logs ea_trading_backend`

### Q3: 看不到數據

**A**:
1. 確認已登入（檢查 localStorage 中的 token）
2. 打開瀏覽器開發者工具（F12）
3. 查看 Network 標籤中的 API 請求
4. 檢查 Console 中的錯誤訊息

### Q4: 跟單沒有執行

**A**:
1. 確認跟單引擎正在運行（儀表板顯示 "Running"）
2. 確認跟單設定已啟用（is_active = true）
3. 檢查是否有未解決的錯誤
4. 查看後端日誌確認對帳邏輯執行

### Q5: npm install 失敗

**A**:
1. 確認 Node.js 版本 >= 16：`node --version`
2. 清除 npm 快取：`npm cache clean --force`
3. 刪除 node_modules 重新安裝：
   ```powershell
   Remove-Item -Recurse -Force node_modules
   npm install
   ```

---

## 開發工具

### 瀏覽器開發者工具

按 F12 打開，可以：
- **Network**: 查看 API 請求和響應
- **Console**: 查看日誌和錯誤
- **Application**: 查看 localStorage 中的 token
- **Elements**: 檢查 DOM 和樣式

### VS Code 擴展推薦

- ES7+ React/Redux/React-Native snippets
- Tailwind CSS IntelliSense
- ESLint
- Prettier

---

## 系統架構圖

```
┌─────────────────┐
│   瀏覽器         │
│  (localhost:    │
│    5173)        │
└────────┬────────┘
         │ HTTP
         ↓
┌─────────────────┐
│  React 前端     │
│  - Dashboard    │
│  - API Client   │
└────────┬────────┘
         │ Axios
         ↓
┌─────────────────┐
│  FastAPI 後端   │
│  (localhost:    │
│    8000)        │
└────────┬────────┘
         │
    ┌────┴────┐
    ↓         ↓
┌────────┐ ┌────────┐
│ Postgre│ │ Redis  │
│  SQL   │ │        │
└────────┘ └────────┘
```

---

## 效能指標

### 後端

- API 響應時間：< 100ms
- 儀表板查詢：< 50ms
- 跟單執行延遲：≤ 3 秒

### 前端

- 首次載入：< 2 秒
- 頁面切換：< 100ms
- 自動刷新間隔：5 秒

---

## 下一步

1. ✅ 測試所有功能
2. ✅ 熟悉系統操作
3. 📝 根據需求自定義 UI
4. 🚀 準備生產環境部署

---

## 相關文檔

- **後端文檔**：
  - `README.md` - 專案總覽
  - `儀表板API測試指南.md` - API 測試
  - `對帳系統實作總結.md` - 對帳邏輯

- **前端文檔**：
  - `frontend/README.md` - 前端專案說明
  - `frontend/啟動前端.md` - 啟動指南
  - `前端實作總結.md` - 實作細節

---

## 技術支援

如遇到問題：
1. 查看相關文檔
2. 檢查瀏覽器 Console 和 Network
3. 查看後端日誌
4. 檢查資料庫數據

---

**祝你使用愉快！** 🎉
