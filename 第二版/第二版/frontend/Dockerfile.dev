# 開發環境 Dockerfile - 支援熱更新
FROM node:18-alpine

# 安裝健康檢查工具
RUN apk add --no-cache wget

WORKDIR /app

# 複製 package.json
COPY package*.json ./

# 安裝依賴
RUN npm install

# 暴露 Vite 開發伺服器端口
EXPOSE 3000

# 啟動開發伺服器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]
